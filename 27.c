#include<stdio.h> 
#include<stdlib.h> 
  
int main() 
{ 
    char str[10],i; 
          scanf("%c",&str[i]);
         if (isdigit(str[i]) != 0) 
         {
             printf("yes");
         }
         else
         {
             printf("no");
         }
  
      return 0; 
} 
